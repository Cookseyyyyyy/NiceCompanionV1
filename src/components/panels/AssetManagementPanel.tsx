import React from 'react';

const AssetManagementPanel: React.FC = () => {
  return (
    <div className="panel-container">
      <h2>Asset Management & Metadata</h2>
      <p>
        Centralize the management of all project assets (video clips, audio, images, effects) 
        with robust tagging and search features. Includes automated tagging, smart search & filtering, 
        versioning of assets, and integration with cloud storage.
      </p>
    </div>
  );
};

export default AssetManagementPanel; 